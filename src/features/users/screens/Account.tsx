import { View } from 'react-native';
import FormAccount from '../components/FormAccount';

const Account = () => {
    return (
        <View>
            <FormAccount />
        </View>
    );
};

export default Account;
